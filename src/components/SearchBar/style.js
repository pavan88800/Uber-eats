import { StyleSheet } from 'react-native'

export const styles = StyleSheet.create({
  searchBarView: {
    marginTop: 20,
    flexDirection: 'row'
  },

  renderLeftButton: {
    marginLeft: 10
  },
  renderRightButton: {
    flexDirection: 'row',
    marginRight: 10,
    backgroundColor: '#fff',
    padding: 9,
    borderRadius: 30,
    alignItems: 'center'
  }
})
