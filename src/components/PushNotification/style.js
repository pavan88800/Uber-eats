import { StyleSheet } from 'react-native'

export const styles = StyleSheet.create({
  input: {
    height: 50,
    margin: 12,
    borderWidth: 1,
    padding: 14,
    width: 300,
    fontSize: 18
  },

  ViewContainer: {
    flex: 1,
    alignItems: 'center',
    justifyContent: 'space-around'
  }
})
